pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
    plugins {
        id 'com.auth0.gradle.oss-library.java' version '0.14.0'
    }
}

rootProject.name = 'auth0'
