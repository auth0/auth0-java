/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.auth0.client.mgmt.types;

import com.auth0.client.mgmt.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import java.io.IOException;
import java.util.Objects;

@JsonDeserialize(using = FlowActionAirtable.Deserializer.class)
public final class FlowActionAirtable {
    private final Object value;

    private final int type;

    private FlowActionAirtable(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    @SuppressWarnings("unchecked")
    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((FlowActionAirtableCreateRecord) this.value);
        } else if (this.type == 1) {
            return visitor.visit((FlowActionAirtableListRecords) this.value);
        } else if (this.type == 2) {
            return visitor.visit((FlowActionAirtableUpdateRecord) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof FlowActionAirtable && equalTo((FlowActionAirtable) other);
    }

    private boolean equalTo(FlowActionAirtable other) {
        return value.equals(other.value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @Override
    public String toString() {
        return this.value.toString();
    }

    public static FlowActionAirtable of(FlowActionAirtableCreateRecord value) {
        return new FlowActionAirtable(value, 0);
    }

    public static FlowActionAirtable of(FlowActionAirtableListRecords value) {
        return new FlowActionAirtable(value, 1);
    }

    public static FlowActionAirtable of(FlowActionAirtableUpdateRecord value) {
        return new FlowActionAirtable(value, 2);
    }

    public interface Visitor<T> {
        T visit(FlowActionAirtableCreateRecord value);

        T visit(FlowActionAirtableListRecords value);

        T visit(FlowActionAirtableUpdateRecord value);
    }

    static final class Deserializer extends StdDeserializer<FlowActionAirtable> {
        Deserializer() {
            super(FlowActionAirtable.class);
        }

        @Override
        public FlowActionAirtable deserialize(JsonParser p, DeserializationContext context) throws IOException {
            Object value = p.readValueAs(Object.class);
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, FlowActionAirtableCreateRecord.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, FlowActionAirtableListRecords.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, FlowActionAirtableUpdateRecord.class));
            } catch (RuntimeException e) {
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
