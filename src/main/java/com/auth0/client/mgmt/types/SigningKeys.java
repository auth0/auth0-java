/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.auth0.client.mgmt.types;

import com.auth0.client.mgmt.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = SigningKeys.Builder.class)
public final class SigningKeys {
    private final String kid;

    private final String cert;

    private final Optional<String> pkcs7;

    private final Optional<Boolean> current;

    private final Optional<Boolean> next;

    private final Optional<Boolean> previous;

    private final Optional<SigningKeysDate> currentSince;

    private final Optional<SigningKeysDate> currentUntil;

    private final String fingerprint;

    private final String thumbprint;

    private final Optional<Boolean> revoked;

    private final Optional<SigningKeysDate> revokedAt;

    private final Map<String, Object> additionalProperties;

    private SigningKeys(
            String kid,
            String cert,
            Optional<String> pkcs7,
            Optional<Boolean> current,
            Optional<Boolean> next,
            Optional<Boolean> previous,
            Optional<SigningKeysDate> currentSince,
            Optional<SigningKeysDate> currentUntil,
            String fingerprint,
            String thumbprint,
            Optional<Boolean> revoked,
            Optional<SigningKeysDate> revokedAt,
            Map<String, Object> additionalProperties) {
        this.kid = kid;
        this.cert = cert;
        this.pkcs7 = pkcs7;
        this.current = current;
        this.next = next;
        this.previous = previous;
        this.currentSince = currentSince;
        this.currentUntil = currentUntil;
        this.fingerprint = fingerprint;
        this.thumbprint = thumbprint;
        this.revoked = revoked;
        this.revokedAt = revokedAt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The key id of the signing key
     */
    @JsonProperty("kid")
    public String getKid() {
        return kid;
    }

    /**
     * @return The public certificate of the signing key
     */
    @JsonProperty("cert")
    public String getCert() {
        return cert;
    }

    /**
     * @return The public certificate of the signing key in pkcs7 format
     */
    @JsonProperty("pkcs7")
    public Optional<String> getPkcs7() {
        return pkcs7;
    }

    /**
     * @return True if the key is the the current key
     */
    @JsonProperty("current")
    public Optional<Boolean> getCurrent() {
        return current;
    }

    /**
     * @return True if the key is the the next key
     */
    @JsonProperty("next")
    public Optional<Boolean> getNext() {
        return next;
    }

    /**
     * @return True if the key is the the previous key
     */
    @JsonProperty("previous")
    public Optional<Boolean> getPrevious() {
        return previous;
    }

    @JsonProperty("current_since")
    public Optional<SigningKeysDate> getCurrentSince() {
        return currentSince;
    }

    @JsonProperty("current_until")
    public Optional<SigningKeysDate> getCurrentUntil() {
        return currentUntil;
    }

    /**
     * @return The cert fingerprint
     */
    @JsonProperty("fingerprint")
    public String getFingerprint() {
        return fingerprint;
    }

    /**
     * @return The cert thumbprint
     */
    @JsonProperty("thumbprint")
    public String getThumbprint() {
        return thumbprint;
    }

    /**
     * @return True if the key is revoked
     */
    @JsonProperty("revoked")
    public Optional<Boolean> getRevoked() {
        return revoked;
    }

    @JsonProperty("revoked_at")
    public Optional<SigningKeysDate> getRevokedAt() {
        return revokedAt;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof SigningKeys && equalTo((SigningKeys) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(SigningKeys other) {
        return kid.equals(other.kid)
                && cert.equals(other.cert)
                && pkcs7.equals(other.pkcs7)
                && current.equals(other.current)
                && next.equals(other.next)
                && previous.equals(other.previous)
                && currentSince.equals(other.currentSince)
                && currentUntil.equals(other.currentUntil)
                && fingerprint.equals(other.fingerprint)
                && thumbprint.equals(other.thumbprint)
                && revoked.equals(other.revoked)
                && revokedAt.equals(other.revokedAt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.kid,
                this.cert,
                this.pkcs7,
                this.current,
                this.next,
                this.previous,
                this.currentSince,
                this.currentUntil,
                this.fingerprint,
                this.thumbprint,
                this.revoked,
                this.revokedAt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static KidStage builder() {
        return new Builder();
    }

    public interface KidStage {
        /**
         * <p>The key id of the signing key</p>
         */
        CertStage kid(@NotNull String kid);

        Builder from(SigningKeys other);
    }

    public interface CertStage {
        /**
         * <p>The public certificate of the signing key</p>
         */
        FingerprintStage cert(@NotNull String cert);
    }

    public interface FingerprintStage {
        /**
         * <p>The cert fingerprint</p>
         */
        ThumbprintStage fingerprint(@NotNull String fingerprint);
    }

    public interface ThumbprintStage {
        /**
         * <p>The cert thumbprint</p>
         */
        _FinalStage thumbprint(@NotNull String thumbprint);
    }

    public interface _FinalStage {
        SigningKeys build();

        /**
         * <p>The public certificate of the signing key in pkcs7 format</p>
         */
        _FinalStage pkcs7(Optional<String> pkcs7);

        _FinalStage pkcs7(String pkcs7);

        /**
         * <p>True if the key is the the current key</p>
         */
        _FinalStage current(Optional<Boolean> current);

        _FinalStage current(Boolean current);

        /**
         * <p>True if the key is the the next key</p>
         */
        _FinalStage next(Optional<Boolean> next);

        _FinalStage next(Boolean next);

        /**
         * <p>True if the key is the the previous key</p>
         */
        _FinalStage previous(Optional<Boolean> previous);

        _FinalStage previous(Boolean previous);

        _FinalStage currentSince(Optional<SigningKeysDate> currentSince);

        _FinalStage currentSince(SigningKeysDate currentSince);

        _FinalStage currentUntil(Optional<SigningKeysDate> currentUntil);

        _FinalStage currentUntil(SigningKeysDate currentUntil);

        /**
         * <p>True if the key is revoked</p>
         */
        _FinalStage revoked(Optional<Boolean> revoked);

        _FinalStage revoked(Boolean revoked);

        _FinalStage revokedAt(Optional<SigningKeysDate> revokedAt);

        _FinalStage revokedAt(SigningKeysDate revokedAt);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements KidStage, CertStage, FingerprintStage, ThumbprintStage, _FinalStage {
        private String kid;

        private String cert;

        private String fingerprint;

        private String thumbprint;

        private Optional<SigningKeysDate> revokedAt = Optional.empty();

        private Optional<Boolean> revoked = Optional.empty();

        private Optional<SigningKeysDate> currentUntil = Optional.empty();

        private Optional<SigningKeysDate> currentSince = Optional.empty();

        private Optional<Boolean> previous = Optional.empty();

        private Optional<Boolean> next = Optional.empty();

        private Optional<Boolean> current = Optional.empty();

        private Optional<String> pkcs7 = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(SigningKeys other) {
            kid(other.getKid());
            cert(other.getCert());
            pkcs7(other.getPkcs7());
            current(other.getCurrent());
            next(other.getNext());
            previous(other.getPrevious());
            currentSince(other.getCurrentSince());
            currentUntil(other.getCurrentUntil());
            fingerprint(other.getFingerprint());
            thumbprint(other.getThumbprint());
            revoked(other.getRevoked());
            revokedAt(other.getRevokedAt());
            return this;
        }

        /**
         * <p>The key id of the signing key</p>
         * <p>The key id of the signing key</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("kid")
        public CertStage kid(@NotNull String kid) {
            this.kid = Objects.requireNonNull(kid, "kid must not be null");
            return this;
        }

        /**
         * <p>The public certificate of the signing key</p>
         * <p>The public certificate of the signing key</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("cert")
        public FingerprintStage cert(@NotNull String cert) {
            this.cert = Objects.requireNonNull(cert, "cert must not be null");
            return this;
        }

        /**
         * <p>The cert fingerprint</p>
         * <p>The cert fingerprint</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("fingerprint")
        public ThumbprintStage fingerprint(@NotNull String fingerprint) {
            this.fingerprint = Objects.requireNonNull(fingerprint, "fingerprint must not be null");
            return this;
        }

        /**
         * <p>The cert thumbprint</p>
         * <p>The cert thumbprint</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("thumbprint")
        public _FinalStage thumbprint(@NotNull String thumbprint) {
            this.thumbprint = Objects.requireNonNull(thumbprint, "thumbprint must not be null");
            return this;
        }

        @java.lang.Override
        public _FinalStage revokedAt(SigningKeysDate revokedAt) {
            this.revokedAt = Optional.ofNullable(revokedAt);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "revoked_at", nulls = Nulls.SKIP)
        public _FinalStage revokedAt(Optional<SigningKeysDate> revokedAt) {
            this.revokedAt = revokedAt;
            return this;
        }

        /**
         * <p>True if the key is revoked</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage revoked(Boolean revoked) {
            this.revoked = Optional.ofNullable(revoked);
            return this;
        }

        /**
         * <p>True if the key is revoked</p>
         */
        @java.lang.Override
        @JsonSetter(value = "revoked", nulls = Nulls.SKIP)
        public _FinalStage revoked(Optional<Boolean> revoked) {
            this.revoked = revoked;
            return this;
        }

        @java.lang.Override
        public _FinalStage currentUntil(SigningKeysDate currentUntil) {
            this.currentUntil = Optional.ofNullable(currentUntil);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "current_until", nulls = Nulls.SKIP)
        public _FinalStage currentUntil(Optional<SigningKeysDate> currentUntil) {
            this.currentUntil = currentUntil;
            return this;
        }

        @java.lang.Override
        public _FinalStage currentSince(SigningKeysDate currentSince) {
            this.currentSince = Optional.ofNullable(currentSince);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "current_since", nulls = Nulls.SKIP)
        public _FinalStage currentSince(Optional<SigningKeysDate> currentSince) {
            this.currentSince = currentSince;
            return this;
        }

        /**
         * <p>True if the key is the the previous key</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage previous(Boolean previous) {
            this.previous = Optional.ofNullable(previous);
            return this;
        }

        /**
         * <p>True if the key is the the previous key</p>
         */
        @java.lang.Override
        @JsonSetter(value = "previous", nulls = Nulls.SKIP)
        public _FinalStage previous(Optional<Boolean> previous) {
            this.previous = previous;
            return this;
        }

        /**
         * <p>True if the key is the the next key</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage next(Boolean next) {
            this.next = Optional.ofNullable(next);
            return this;
        }

        /**
         * <p>True if the key is the the next key</p>
         */
        @java.lang.Override
        @JsonSetter(value = "next", nulls = Nulls.SKIP)
        public _FinalStage next(Optional<Boolean> next) {
            this.next = next;
            return this;
        }

        /**
         * <p>True if the key is the the current key</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage current(Boolean current) {
            this.current = Optional.ofNullable(current);
            return this;
        }

        /**
         * <p>True if the key is the the current key</p>
         */
        @java.lang.Override
        @JsonSetter(value = "current", nulls = Nulls.SKIP)
        public _FinalStage current(Optional<Boolean> current) {
            this.current = current;
            return this;
        }

        /**
         * <p>The public certificate of the signing key in pkcs7 format</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage pkcs7(String pkcs7) {
            this.pkcs7 = Optional.ofNullable(pkcs7);
            return this;
        }

        /**
         * <p>The public certificate of the signing key in pkcs7 format</p>
         */
        @java.lang.Override
        @JsonSetter(value = "pkcs7", nulls = Nulls.SKIP)
        public _FinalStage pkcs7(Optional<String> pkcs7) {
            this.pkcs7 = pkcs7;
            return this;
        }

        @java.lang.Override
        public SigningKeys build() {
            return new SigningKeys(
                    kid,
                    cert,
                    pkcs7,
                    current,
                    next,
                    previous,
                    currentSince,
                    currentUntil,
                    fingerprint,
                    thumbprint,
                    revoked,
                    revokedAt,
                    additionalProperties);
        }
    }
}
