/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.auth0.client.mgmt.types;

import com.auth0.client.mgmt.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import java.io.IOException;
import java.util.Objects;

@JsonDeserialize(using = FormComponent.Deserializer.class)
public final class FormComponent {
    private final Object value;

    private final int type;

    private FormComponent(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    @SuppressWarnings("unchecked")
    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((FormBlock) this.value);
        } else if (this.type == 1) {
            return visitor.visit((FormWidget) this.value);
        } else if (this.type == 2) {
            return visitor.visit((FormField) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof FormComponent && equalTo((FormComponent) other);
    }

    private boolean equalTo(FormComponent other) {
        return value.equals(other.value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @Override
    public String toString() {
        return this.value.toString();
    }

    public static FormComponent of(FormBlock value) {
        return new FormComponent(value, 0);
    }

    public static FormComponent of(FormWidget value) {
        return new FormComponent(value, 1);
    }

    public static FormComponent of(FormField value) {
        return new FormComponent(value, 2);
    }

    public interface Visitor<T> {
        T visit(FormBlock value);

        T visit(FormWidget value);

        T visit(FormField value);
    }

    static final class Deserializer extends StdDeserializer<FormComponent> {
        Deserializer() {
            super(FormComponent.class);
        }

        @Override
        public FormComponent deserialize(JsonParser p, DeserializationContext context) throws IOException {
            Object value = p.readValueAs(Object.class);
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, FormBlock.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, FormWidget.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, FormField.class));
            } catch (RuntimeException e) {
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
